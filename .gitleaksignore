# Znaleziska gitleaks uznane za FAŁSZYWE ALARMY — każde z uzasadnieniem.
#
# Format wpisu: <commit>:<plik>:<reguła>:<linia>
# Fingerprint wskazuje JEDNO wystąpienie w JEDNYM commicie, więc nowy sekret
# w tym samym pliku nadal zapala skan. Sprawdzone testem negatywnym
# (2026-09-01): po podłożeniu klucza do smoke-wp-jezyk.mjs skan znalazł
# 2 zamiast 1 — czyli wyciszenie nie oślepia pliku.
#
# ⚠️ PUŁAPKA DRUGA, ZMIERZONA 2026-09-07: FINGERPRINT ZAWIERA SHA COMMITA,
# więc KAŻDE PRZEPISANIE HISTORII (git-filter-repo, rebase, squash) unieważnia
# WSZYSTKIE wpisy w tym pliku i skan zapala się na nowo — mimo że nic się nie
# zmieniło w treści. Zdarzyło się przy usuwaniu katalogu `d7/cytowane/`
# 2026-09-07: te same dwa pliki, te same reguły, te same linie, nowe SHA.
# Po każdym przepisaniu historii trzeba przeliczyć odciski:
#   gitleaks git --no-banner --redact --report-format json \
#     --report-path /tmp/leaks.json --exit-code 1 .
# i przepisać wartości pola `Fingerprint` do wpisów niżej.
#
# PUŁAPKA POMIARU: fingerprinty różnią się między trybami gitleaksa.
# Ten plik działa dla `gitleaks git` (tego używa CI, job „Skan sekretów”).
# Lokalny `gitleaks dir` liczy fingerprint BEZ commita, więc mimo tego wpisu
# nadal zgłosi znalezisko — to nie jest regresja, tylko inny tryb.
#
# ── generic-api-key w tools/smoke/smoke-wp-jezyk.mjs ──────────────────────
# Tablica FRAZY to lista ANGIELSKICH napisów interfejsu WooCommerce, których
# bramka szuka na żywej stronie, żeby udowodnić, że klient widzi polszczyznę
# (BLAD-024, naprawiony w 0.51.0). Gitleaks dopasował sąsiadujące pozycje
# „Password change” i „Country/Region” jako klucz API. To nazwy pól
# formularza, nie sekret — plik nie zawiera i nie zawierał żadnych danych
# uwierzytelniających.
f825473222fe2093d7dbbcc3b1285625347d0a2e:tools/smoke/smoke-wp-jezyk.mjs:generic-api-key:101

# ── generic-api-key w audyt/wyniki/kontrola-0.65.0/PRIV.md ────────────────
# Dział PRIV fali kontrolnej 0.65.0 rozlicza się w raporcie z KAŻDEGO wiersza,
# który po sobie zostawił. Jednym z nich była sesja gościa WooCommerce, więc
# w tabeli „kod wyjścia” stoi dosłowna komenda, która ją skasowała — razem
# z 30-znakowym `session_key`. Gitleaks widzi ciąg heksadecymalny przy słowie
# `key` i dopasowuje go jako klucz API.
#
# DLACZEGO TO NIE JEST SEKRET — zmierzone, nie założone:
#   1. to identyfikator sesji GOŚCIA z lokalnego warsztatu `127.0.0.1:8892`,
#      który nigdy nie był publiczny;
#   2. wiersz JUŻ NIE ISTNIEJE — skasowała go ta sama komenda, którą cytuje
#      dokument. Sprawdzone zapytaniem: `SELECT COUNT(*) FROM
#      wp_woocommerce_sessions WHERE session_key='t_d101…'` zwraca `0`;
#   3. wartość nie występuje w ŻADNYM pliku drzewa roboczego (`grep -r`
#      po całym repo poza `.git`) — żyje wyłącznie w historii gita, więc
#      wyciszenie fingerprintu jest jedyną drogą bez przepisywania historii
#      wypchniętej gałęzi sektora.
#
# Sesja WooCommerce nie uwierzytelnia konta ani usługi: to koszyk gościa.
# Gdyby kiedyś doszedł tu wiersz z prawdziwym sekretem, fingerprint jest
# przypięty do JEDNEGO commita i JEDNEJ linii, więc skan zapali się na nowo.
a13042209af6b4312cb319974c4e040caaa3be94:audyt/wyniki/kontrola-0.65.0/PRIV.md:generic-api-key:64
