# ROZSTRZYGNIĘCIE WŁAŚCICIELA 2026-09-02 (pakiet E7.7, pozycja 4, pytanie 4):
# `stan/` i `migawki/` WCHODZĄ DO GITA. Wcześniejszy zapis (STRUKTURA.md, „Wpis
# PRÓBNY": do gita wyłącznie zgloszenia/) przestał być prawdziwy, gdy fala 2
# dostała własny worktree ze sparse checkoutem: bez gita stan fali 2 nie ma jak
# wrócić do głównego drzewa, `porownaj-cykle --dzial=` nie widzi ZAKOŃCZONE
# fali 2, a dziennik wejść (historia przejść, pozycja 3) nie jest dowodem.
# Cztery pliki stanu z prób E6/E7.6 i obie migawki weszły jako materiał dowodowy.
# Reguła 28 strażnika sektora pilnuje, żeby te dwa katalogi NIE wróciły tutaj.
